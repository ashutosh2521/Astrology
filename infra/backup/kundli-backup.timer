[Unit]
Description=Run the Kundli SQLite backup nightly

[Timer]
# 02:30 host time every day, with a randomized delay so it isn't exactly on the minute.
OnCalendar=*-*-* 02:30:00
RandomizedDelaySec=15min
Persistent=true

[Install]
WantedBy=timers.target
