# Ephemeris microservice dependencies. Pinned for reproducibility (no Docker;
# a pinned requirements set + documented system packages is our reproducibility story).
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
pyswisseph==2.10.3.2
